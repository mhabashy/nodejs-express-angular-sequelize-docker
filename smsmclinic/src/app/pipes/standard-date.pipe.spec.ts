import { StandardDatePipe } from './standard-date.pipe';

describe('StandardDatePipe', () => {
  it('create an instance', () => {
    const pipe = new StandardDatePipe();
    expect(pipe).toBeTruthy();
  });
});
